apply plugin: "war"

bootWar {
    mainClassName = "com.enterprise.app.EnterpriseappApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
